name: Code Quality Scan

on:
  workflow_call:
  push:
    branches:
      - main
  pull_request:
    branches: [ main ]

jobs:
  CodeQuality:
    uses: TheDigitalSages/tds-github-actions/.github/workflows/CodeQuality.yml@feature/actions
    secrets: inherit
  Testing:
    uses: TheDigitalSages/tds-github-actions/.github/workflows/WebFrontEndTests.yml@feature/actions
    secrets: inherit